services:
  treasury-risk-api:
    image: localhost/treasury-risk-api:latest
    container_name: treasury-risk-api
    restart: unless-stopped
    security_opt:
      - seccomp:unconfined
      - label:disable
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000