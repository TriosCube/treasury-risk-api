version: '3.8'

services:
  treasury-risk-api:
    build: .
    container_name: treasury-risk-api
    restart: unless-stopped
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000